let frutas = ['platano','manzana','pera','uva'];
frutas[1];
frutas.push('papaya') // ingresara una elemento al array
frutas.pop // elimina el ultimo elemento
frutas.unshift('durazno'); // agrega elemento al principio
frutas.shift(); // borra el primer elemento
frutas.indexOf('platano');//devolver la posicion del elemento
